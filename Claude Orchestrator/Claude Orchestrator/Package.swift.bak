// swift-tools-version: 5.9
import PackageDescription

let package = Package(
    name: "ClaudeTerminal",
    platforms: [
        .iOS(.v17)
    ],
    dependencies: [
        .package(
            url: "https://github.com/migueldeicaza/SwiftTerm.git",
            from: "1.2.2"
        )
    ],
    targets: [
        .target(
            name: "ClaudeTerminal",
            dependencies: [
                .product(name: "SwiftTerm", package: "SwiftTerm")
            ],
            path: ".",
            sources: ["App", "Views", "Models", "Services"]
        )
    ]
)
